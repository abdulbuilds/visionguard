# ByteTrack tracker configuration for TrafficSight AI
# track_buffer=30 keeps tracks alive for ~30 frames during short occlusions

tracker_type: bytetrack
track_high_thresh: 0.25
track_low_thresh: 0.1
new_track_thresh: 0.25
track_buffer: 30
match_thresh: 0.8
fuse_score: True
